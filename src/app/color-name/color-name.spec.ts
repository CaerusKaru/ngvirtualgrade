import { ColorNameDirective } from './color-name';

describe('ColorNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNameDirective();
    expect(directive).toBeTruthy();
  });
});
