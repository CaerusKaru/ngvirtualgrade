import { SvgTextDirective } from './svg-text.directive';

describe('SvgTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgTextDirective();
    expect(directive).toBeTruthy();
  });
});
