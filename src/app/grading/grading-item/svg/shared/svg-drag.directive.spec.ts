import { SvgDragDirective } from './svg-drag.directive';

describe('SvgDragDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgDragDirective();
    expect(directive).toBeTruthy();
  });
});
