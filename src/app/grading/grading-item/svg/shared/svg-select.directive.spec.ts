import { SvgSelectDirective } from './svg-select.directive';

describe('SvgSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgSelectDirective();
    expect(directive).toBeTruthy();
  });
});
