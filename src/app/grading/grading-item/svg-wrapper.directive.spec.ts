import { SvgWrapperDirective } from './svg-wrapper.directive';

describe('SvgWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
